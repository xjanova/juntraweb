# CLAUDE.md — จันทราพยากรณ์ (juntraweb)

## 🔴 กฎเหล็ก: งานโหราศาสตร์ต้องคำนวณจริง ห้ามเดา

เว็บนี้ขายคำพยากรณ์ ลูกค้าจ่ายเงินจริง และ **ฤกษ์ที่ผิดลูกค้าไม่มีทางรู้ได้เอง**
(ต่างจากหน้าเว็บพังซึ่งเห็นทันที) ตัวเลขที่มั่วขึ้นมาตัวเดียวจึงร้ายแรงกว่าบั๊กปกติ
กฎหมวดนี้อยู่เหนือความอยากให้ฟีเจอร์เสร็จ และเหนือความสวยงามของโค้ด

### 1. ค่าดาราศาสตร์ต้องคำนวณ ไม่ใช่แต่งหรือสุ่ม

- ตำแหน่งดาว · ดิถี · นักษัตร · ฤกษ์บน · ยาม ต้องมาจาก `App\Support\ThaiAstro`
  (สูตร Meeus, *Astronomical Algorithms*) เท่านั้น ห้ามคำนวณเองซ้ำในที่อื่น
- ห้ามใช้ `rand()` / `crc32()` / hash แทนค่าดาราศาสตร์ — ใช้ได้เฉพาะของประดับ
  ที่ไม่มีใครอ้างว่าเป็นผลคำนวณ (สีนำโชค เลขนำโชค ไพ่ประจำวัน)
- ห้ามใช้ heuristic ปลอมเป็นหลักโหร บั๊กเดิมที่เคยหลุดขึ้น prod:
  ให้คะแนนวันจาก "เป็นวัน อ./พฤ./ส. ไหม + เป็นวันที่ 9/19/29 ไหม" ซึ่งไม่มีในตำราเลย
  ทำให้ถามเรื่องแต่งงานกับเปิดร้านได้วันชุดเดียวกันเป๊ะ
- ห้ามให้ AI แต่งเนื้อหาแทนการคำนวณ ถ้าระบบมีเครื่องคำนวณอยู่แล้วต้องเรียกใช้จริง

### 2. ตัวเลข/ตารางจากตำรา ต้อง verify หลายแหล่งที่ตรงกัน แล้วจดแหล่งไว้

ตารางลงเลขยาม ชื่อยาม ลำดับฤกษ์ ฯลฯ **คือ "ตำรา" ไม่ใช่ตัวเลือกของเรา**
ต้องหาแหล่งที่ตรงกันอย่างน้อย 2–3 เจ้าก่อนเขียนลงโค้ด แล้วจดว่าเอามาจากไหน
ไว้ในคอมเมนต์หรือ docblock ของเทสต์ (ดูตัวอย่างที่ `tests/Unit/ThaiAstroTest.php`
ซึ่งตรึงลำดับเลขยามทั้ง 7 วารไว้ พร้อมเหตุผลว่าทำไมต้องตรึง)

### 3. แหล่งขัดกัน หรือหาไม่ได้ → **ไม่ทำ** แล้วบอกว่าทำไม

นี่คือหัวใจของกฎข้อนี้: **ยอมทำน้อยแต่ถูก ดีกว่าทำครบแต่มั่ว**

เมื่อหาตำราที่เชื่อถือได้ไม่เจอ หรือแหล่งขัดกันเอง ให้หยุด แล้วรายงานตรง ๆ ว่า
ทำไม่ได้เพราะอะไร ขัดกันตรงไหน ต้องได้อะไรเพิ่มถึงจะทำได้ — ห้ามเดาแล้วส่งงาน
และห้ามเขียนโค้ดที่ "ดูเหมือนคำนวณ" ทั้งที่ค่าข้างในแต่งขึ้น

ตัวอย่างจริงที่ตัดสินใจแบบนี้แล้วและถือเป็นบรรทัดฐาน:

- **ตาราง คู่มิตร/คู่ศัตรู ของดาว** — แหล่งขัดกันเอง (บางเจ้าให้ ๒–๕ เป็นทั้งคู่ธาตุ
  และคู่ศัตรู) จึงไม่แต่งขึ้นใหม่ ใช้ตารางน้ำหนักวารเดิมของ `AuspiciousOccasions` แทน
- **วันธงชัย/อธิบดี/อุบาทว์/โลกาวินาศ** — จงใจไม่ทำ เพราะผูกกับเดือนจันทรคติไทย
  ตามปฏิทินหลวง (มีอธิกมาส/อธิกวารที่ประกาศเป็นปี ๆ ไป) เดาเดือนผิดไปเดือนเดียว
  ตารางทั้งชุดผิดหมด — ถ้าจะทำ ต้องมีปฏิทินหลวงจริงมาก่อน ไม่ใช่คำนวณเดา
- **ยามอัฐกาล** — ทำได้เพราะหาแหล่งที่ตรงกันครบสามเจ้า (ยืนยันสูตร +๕/+๔
  และช่องแรก = ดาวเจ้าวัน) ถ้าหาไม่ได้ก็จะไม่ทำเหมือนสองข้อบน

### 4. ต้องพิสูจน์ความแม่นกับแหล่งภายนอก แล้วตรึงไว้ในเทสต์

ความถูกต้องต้องตรวจสอบได้ ไม่ใช่เชื่อเพราะโค้ดรันผ่าน — ดิถี/เฟสดวงจันทร์เทียบกับ
เวลาจันทร์ดับ/เพ็ญจริงของ NASA (Espenak) ปัจจุบันคลาดเคลื่อน 0.011–0.019°
ตรึงไว้ที่ `test_moon_sun_elongation_matches_nasa_syzygy_times`
ของใหม่ที่เพิ่มเข้ามาต้องมีหมุดแบบเดียวกันเสมอ

### 5. เทสต์ห้ามเดาวันที่ ต้องตรึงวันและคำนวณคุณสมบัติของวันนั้นจริง

- ห้ามทดสอบด้วย `Carbon::today()` เพียงอย่างเดียวสำหรับอะไรที่ขึ้นกับดิถี/วาร
  ผลจะเขียว-แดงสลับตามวันที่รัน ไม่ใช่ตามความถูกต้องของโค้ด
  ให้ตรึงด้วย `Carbon::setTestNow()` และเลือกวันให้ครอบทุกกิ่งเงื่อนไข
- **ห้ามเดาว่าวันไหนเป็นข้างขึ้น/ข้างแรม** ให้คำนวณจาก `ThaiAstro::tithi()`
  แล้ว assert คุณสมบัติที่คาดไว้ก่อนใช้วันนั้นทดสอบ ถ้าเดาผิดจะได้รู้ทันที
  (เกิดขึ้นจริงแล้ว: 2026-06-20 ถูกเดาว่าข้างแรม แต่จริง ๆ เป็นข้างขึ้น)
- ดิถีบางค่าไม่ได้มีทุกเดือน (ปี 2026 ไม่มีดิถี 30 เลยทั้งเดือนมิถุนายน)
  การกวาดช่วงต่อเนื่องอย่างเดียวจึงพลาดกิ่งได้ — ต้องตรึงวันเฉพาะเพิ่ม
  และ assert ว่าช่วงที่กวาดครอบกิ่งที่ตั้งใจจริง

### 6. คำพยากรณ์รายบุคคลต้องมี "ของเฉพาะคนนั้น" ทุกช่อง

บั๊กที่หลุดมาแล้วสองรอบ: ข้อความประกอบจากธาตุอย่างเดียว ทำให้ราศีที่ธาตุเดียวกัน
(เมษ/สิงห์/ธนู) อ่านได้ข้อความเดียวกันเป๊ะ — 12 ราศีเหลือ 4 แบบ
ทุกกิ่งของทุกช่องต้องแทรกสิ่งที่เป็นของราศีนั้นจริง ๆ (เช่น `$zodiac->name_th`)
ไม่ใช่ของรายธาตุ และต้องมีเทสต์ยืนยันว่าต่างกันครบทั้ง 12 ราศี **ทุกวัน** ไม่ใช่วันนี้วันเดียว

---

## คำสั่งที่ใช้บ่อย

```bash
php artisan test --filter=DailyHoroscopeTest
```

PHP ของเครื่องนี้: `C:\laragon\bin\php\php-8.3.26-Win32-vs16-x64\php.exe`
