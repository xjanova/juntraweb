/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.local
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Deployment artifacts (keep .env.production as a template — server fills it)
.deploy-backup/
storage/logs/deployment.log
storage/logs/*.log
storage/app/public/palmistry/

# Local SQLite (production uses MySQL)
database/database.sqlite

# Imported tarot card art — too large for git, re-imported on the server
# via the Filament admin "Import 78 ใบ จากเครื่องนี้" action.
/public/images/tarot/

# Marker file written by the first-time installer
/storage/app/.installed

# Editor / tooling caches
/.claude/

# Local theme working files (not part of app)
/_theme_extracted/
/_run.ps1
*.zip

# preview sandbox (sqlite) — ไม่ commit
.env.preview
